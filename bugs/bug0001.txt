Через несколько запросов New Hand перестаёт работать. Суть проблемы пока не ясна.

Копились pending POST запросы на сервер. Нужно было явно прописать позитивный статус код для ответа.

Проблема решена.